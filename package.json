{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run clean && eleventy",
    "clean": "rm -rf build",
    "start": "npm run clean && eleventy --port 3000 --serve",
    "test": "eslint static"
  },
  "dependencies": {
    "@11ty/eleventy": "^2.0.0-beta.1",
    "@11ty/eleventy-plugin-syntaxhighlight": "^4.2.0",
    "html-minifier": "^4.0.0"
  },
  "devDependencies": {
    "eslint": "8.32",
    "eslint-config-google": "^0.14.0"
  }
}
