---
import Base from '../layouts/Base.astro'
import { getEntry, render } from 'astro:content'

const entry = await getEntry('pages', 'styleguide')
const { Content } = await render(entry)
---
<Base title={entry.data.title}>
  <article>
    <Content />
  </article>
</Base>
